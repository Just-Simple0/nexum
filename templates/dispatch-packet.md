# Dispatch Packet

Use this as the sole Builder prompt whenever a task contract is larger than a compact execution brief. Target 350 words or fewer, excluding exact commands and code snippets. Exceed that only with an explicit contract rationale.

- Run/task:
- Execution class: direct | bounded | context-heavy
- Builder model / effort:
- Owned paths:

## Objective

## Acceptance criteria

- [ ]

## Constraints and stop conditions

## Exact commands to run

## Context files to inspect

List only the files the Builder needs initially. Do not attach policies, prior reasoning, reviewer findings, full run logs, or unrelated repository files.
